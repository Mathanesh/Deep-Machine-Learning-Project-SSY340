# Install required packages
pip install numpy matplotlib pandas 
pip install torchinfo 
pip install opencv-python opencv-contrib-python 
pip install scikit-learn 

# For CPU (Non-CUDA Support)
pip install torch==2.3.1 torchvision --index-url https://download.pytorch.org/whl/cpu          # System Installation
pip install --user  torch==2.3.1 torchvision --index-url https://download.pytorch.org/whl/cpu  # User Installation
pip install torchvision==0.18.1+cpu --index-url https://download.pytorch.org/whl/cpu           # Only torchvision

# For GPU (CUDA Support)
pip install torch==2.3.1+cu118 torchvision --index-url https://download.pytorch.org/whl/cu118

# Default Available Segmentation Models
pip install segmentation-models-pytorch            # System Installation
pip install --user segmentation-models-pytorch     # User Installation
 
+ other required packages